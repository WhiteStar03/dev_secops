name: WordPress WPScan DevSecOps Loop

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    name: Scan ${{ matrix.profile }} profile
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        profile:
          - vulnerable
          - fixed

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Start WordPress stack
        run: ./scripts/up-stack.sh "${{ matrix.profile }}" "ci-${{ matrix.profile }}"

      - name: Wait for WordPress
        run: ./scripts/wait-for-wordpress.sh "http://127.0.0.1:8090/wp-admin/install.php"

      - name: Install WordPress
        run: ./scripts/install-wordpress.sh

      - name: Run WPScan
        env:
          WPSCAN_API_TOKEN: ${{ secrets.WPSCAN_API_TOKEN }}
        run: ./scripts/run-wpscan.sh "${{ matrix.profile }}" "github-actions-${{ github.run_id }}"

      - name: Upload scan artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: wpscan-${{ matrix.profile }}-${{ github.run_id }}
          path: scans/${{ matrix.profile }}/
          if-no-files-found: error

      - name: Stop WordPress stack
        if: always()
        run: ./scripts/down-stack.sh "${{ matrix.profile }}" "ci-${{ matrix.profile }}"

  publish-fixed-image-ghcr:
    name: Publish fixed image to GHCR
    runs-on: ubuntu-latest
    needs: scan
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push fixed image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/wordpress/Dockerfile
          target: fixed
          push: true
          tags: |
            ghcr.io/whitestar03/wordpress-devsecops:latest
            ghcr.io/whitestar03/wordpress-devsecops:${{ github.sha }}

  publish-fixed-image-dockerhub:
    name: Publish fixed image to Docker Hub
    runs-on: ubuntu-latest
    needs: scan
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: read
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Set up Buildx
        if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Build and push fixed image to Docker Hub
        if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/wordpress/Dockerfile
          target: fixed
          push: true
          tags: |
            ${{ env.DOCKERHUB_USERNAME }}/wordpress-devsecops:latest
            ${{ env.DOCKERHUB_USERNAME }}/wordpress-devsecops:${{ github.sha }}

      - name: Skip Docker Hub publishing
        if: env.DOCKERHUB_USERNAME == '' || env.DOCKERHUB_TOKEN == ''
        run: echo "Docker Hub secrets are not configured; skipping Docker Hub push."

  build-report:
    name: Build PDF report
    runs-on: ubuntu-latest
    needs: scan
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Build PDF report
        run: ./scripts/build-report.sh

      - name: Upload PDF report
        uses: actions/upload-artifact@v7.0.1
        with:
          name: report-pdf-${{ github.run_id }}
          path: report/report.pdf
          if-no-files-found: error
